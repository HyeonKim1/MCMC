prior:

  theta0: 
    type: 'gauss'        # if type is 'uni', range is [lower bound, upper bound]
    range: [4, 1]      # if type is 'gauss', range is [mean, standard deviation]

  theta1: 
    type: 'gauss'      # if type is 'uni', range is [lower bound, upper bound]
    range: [7, 1]  # if type is 'gauss', range is [mean, standard deviation]
